# Shotline 交互原型

在仓库根目录运行 `python3 -m http.server 4317 --bind 127.0.0.1 --directory demo`，打开 http://127.0.0.1:4317 。无需安装依赖。

内置固定场景，可选择、重排、删除和重置镜头，调整镜头参数，编辑/复制提示词，导出 JSON。不会调用模型、分析任意输入、生成图片或视频；质检需要人工完成。插图是预制示例资产。刷新页面会清空编辑，请先导出。
